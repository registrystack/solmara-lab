name: child-benefit-household-match
classification: synthetic
request:
  target:
    type: Person
    identifiers: [{ scheme: solmara_uin, value: "2300010248" }]
  claims: [household-below-poverty-threshold]
  disclosure: predicate
  format: application/vnd.registry-notary.claim-result+json
  purpose: https://id.registrystack.org/solmara/purpose/child-benefit-review
input: { uin: "2300010248" }
interactions:
  - expect: { method: GET, path: /snapshot }
    respond: { status: 200, body: { poverty_band: priority } }
expect:
  outcome: match
  outputs: { poverty_band: priority }
  claims: { household-below-poverty-threshold: true }
