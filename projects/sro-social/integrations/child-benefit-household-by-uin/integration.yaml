version: 1
id: child-benefit-household-by-uin
revision: 1

input:
  uin:
    role: selector
    type: string
    minLength: 10
    maxLength: 10
    pattern: "^[0-9]{10}$"

capability:
  snapshot:
    entity: sro_child
    exact:
      uin: { input: uin }
    freshness: 31d

outputs: [poverty_band]

not_applicable:
  ambiguity:
    rationale: The exact UIN selector is the entity primary key, whose materialized unique-key constraint permits at most one active household projection.
    request_fixture: child-benefit-household-match
  subject_mismatch:
    rationale: The minimized output projection omits UIN, so it contains no identifier comparable with the requested UIN.
    request_fixture: child-benefit-household-match
