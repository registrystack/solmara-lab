name: nia-population-person-match
classification: synthetic
input: { uin: "2300010248" }
interactions:
  - expect: { method: GET, path: /snapshot }
    respond:
      status: 200
      body: { identity_status: active, alive: true }
expect:
  outcome: match
  outputs: { identity_status: active, alive: true }
  claims:
    population-record-active: true
    citizen-population-record-active: true
