# Solmara Lab change: walt's stock value is `false`, which resolves did:web
# over plain HTTP. Authority Notaries issue credentials with HTTPS did:web
# identifiers, so the wallet must resolve those DIDs over HTTPS to verify and
# display the credentials.
enableDidWebResolverHttps=true
