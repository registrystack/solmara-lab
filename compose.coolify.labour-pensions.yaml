name: solmara-lab-labour-pensions

x-relay-environment: &relay-environment
  RUST_LOG: ${RUST_LOG:-info}
  REGISTRY_RELAY_BIND: 0.0.0.0:8080

x-relay-common: &relay-common
  image: ${SOLMARA_RELAY_IMAGE:?set digest-pinned Solmara Relay image}
  platform: ${REGISTRY_STACK_PLATFORM:-linux/amd64}
  restart: unless-stopped
  environment: *relay-environment
  healthcheck:
    test: ["CMD", "/usr/local/bin/registry-relay", "healthcheck"]
    interval: 30s
    timeout: 10s
    retries: 3

services:
  postgres:
    image: ${SOLMARA_POSTGRES_IMAGE:?set digest-pinned Solmara Postgres image}
    entrypoint: ["/bin/sh", "/usr/local/bin/solmara-postgres-entrypoint.sh"]
    command: ["postgres", "-c", "ssl=on", "-c", "ssl_cert_file=/var/lib/postgresql/server-ssl/server.crt", "-c", "ssl_key_file=/var/lib/postgresql/server-ssl/server.key"]
    environment:
      POSTGRES_USER: ${SOLMARA_POSTGRES_USER:-solmara_registry}
      POSTGRES_PASSWORD: ${SOLMARA_POSTGRES_PASSWORD}
      POSTGRES_DB: ${SOLMARA_POSTGRES_DB:-solmara_lab}
      SOLMARA_SOURCE_READER_DATABASES: sipf
      SIPF_SOURCE_POSTGRES_READER_PASSWORD: ${SIPF_SOURCE_POSTGRES_READER_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "test -f /tmp/solmara-postgres-provisioned && pg_isready -h 127.0.0.1 -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  sipf-evidence-workload-agent:
    image: ${SOLMARA_SCENARIO_RUNNER_IMAGE:?set digest-pinned Solmara scenario runner image}
    command: ["python", "scripts/relay_workload_identity_agent.py"]
    restart: unless-stopped
    environment:
      WORKLOAD_ISSUER: https://workload-issuer.solmara.registrystack.org
      WORKLOAD_IDENTITIES_JSON: >-
        [{"audience":"registry-relay","azp":"solmara-evidence","subject":"solmara-evidence","scopes":["pension:rows","survivor:rows"],"token_file":"/run/secrets/sipf-relay-token","private_jwk_env":"SIPF_RELAY_WORKLOAD_JWK","token_uid":65532,"token_gid":65532}]
      SIPF_RELAY_WORKLOAD_JWK: ${SIPF_RELAY_WORKLOAD_JWK}
    volumes:
      - sipf-evidence-workload-token:/run/secrets
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8090/health', timeout=3)\""]
      interval: 10s
      timeout: 5s
      retries: 6

  sipf-relay-config-state-init:
    image: ${SOLMARA_RELAY_IMAGE:?set digest-pinned Solmara Relay image}
    platform: ${REGISTRY_STACK_PLATFORM:-linux/amd64}
    user: "0:0"
    restart: "no"
    entrypoint: ["/bin/busybox", "sh", "-eu", "-c"]
    command:
      - |
        /bin/busybox mkdir -p /var/lib/registry-relay/cache
        /bin/busybox chown 65532:65532 /var/lib/registry-relay/cache
        if ! /bin/busybox test -e /var/lib/registry-relay/cache/config-bundle-antirollback.json; then
          /bin/busybox cp /etc/solmara/hosted-relay-bundles/sipf-pensions/antirollback-seed.json /var/lib/registry-relay/cache/config-bundle-antirollback.json
          /bin/busybox chown 65532:65532 /var/lib/registry-relay/cache/config-bundle-antirollback.json
          /bin/busybox chmod 0600 /var/lib/registry-relay/cache/config-bundle-antirollback.json
        fi
    volumes:
      - sipf-relay-cache:/var/lib/registry-relay/cache

  sipf-pensions-relay:
    <<: *relay-common
    command: ["--config", "/etc/solmara/hosted-relay-bundles/sipf-pensions/bootstrap.yaml"]
    environment:
      <<: *relay-environment
      REGISTRY_RELAY_AUDIT_HASH_SECRET: ${SIPF_RELAY_AUDIT_HASH_SECRET}
      SOLMARA_SIPF_DATABASE_URL: ${SOLMARA_SIPF_DATABASE_URL}
      DATA_GATE_POSTGRES_ROOT_CERT_PATH: /etc/solmara/postgres/root.crt
    expose: ["8080"]
    volumes:
      - sipf-relay-cache:/var/lib/registry-relay/cache
    depends_on:
      sipf-relay-config-state-init: { condition: service_completed_successfully }
      postgres: { condition: service_healthy }
    labels:
      solmara.lab.host: sipf-relay.solmara.registrystack.org

volumes:
  postgres-data:
  sipf-relay-cache:
  sipf-evidence-workload-token:
    name: ${SIPF_EVIDENCE_WORKLOAD_TOKEN_VOLUME:-solmara-sipf-evidence-workload-token}
