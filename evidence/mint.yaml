version: 1
validationMode: supervised-local-development
issuer: https://mint.evidence.solmara.invalid
listener:
  address: 0.0.0.0
  port: 8081
signing:
  algorithm: ES256
  activePublicJwkFile: public-keys/active.jwk.json
  publishedPublicJwkFiles: []
  revokedKeyIds: []
signer:
  kind: local-jwk
  privateKeyRef: secret:file/signing.jwk
secretProviders:
  file:
    root: /run/secrets/registry-mint
audit:
  path: /var/lib/registry-mint/audit/mint.jsonl
  maximumFileBytes: 1073741824
  hashKeyRef: secret:file/audit-hmac-key
  hashKeyVersion: 1
accessTokens:
  audiences: [solmara-evidence]
  lifetimeSeconds: 300
  claims:
    principal: sub
    requesterTags: evidence_tags
    evidenceAudience: evidence_audience
    grantId: evidence_grant_id
    grantAuthority: evidence_authority
clientAssertion:
  audience: https://mint.evidence.solmara.invalid/token
  maximumLifetimeSeconds: 300
  algorithms: [EdDSA]
clients:
  directory: /etc/registry-mint/clients
