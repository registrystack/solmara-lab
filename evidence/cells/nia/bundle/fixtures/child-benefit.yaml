fixture: registry.evidence.reference.solmara.nia-child-benefit/v1
synthetic_only: true
common:
  observed_at: '2026-07-14T00:00:00Z'
  selectors: { subject: { profile: solmara-uin-v1, values: { uin: '1000000004' } } }
  derivationSelectorInputs: {}
  extract: |
    CREATE TABLE evidence_extract (published_at TEXT NOT NULL, publisher TEXT NOT NULL, extract_id TEXT NOT NULL);
    INSERT INTO evidence_extract VALUES ('2026-07-14T00:00:00Z', 'did:web:id.registrystack.org:solmara:authority:nia', 'nia-population-2026-07-14');
    CREATE TABLE population_evidence (uin TEXT NOT NULL, identity_status TEXT NOT NULL, alive INTEGER NOT NULL);
    INSERT INTO population_evidence VALUES ('1000000004', 'active', 1);
    INSERT INTO population_evidence VALUES ('1000000014', 'inactive', 1);
    INSERT INTO population_evidence VALUES ('1000000016', 'active', 1);
    INSERT INTO population_evidence VALUES ('1000000016', 'inactive', 1);
  expectedRequestParts: { parameters: {} }
  expectedTransport: { statement: queries/population-evidence.sql }
cases:
  - id: positive
    selectors: { subject: { profile: solmara-uin-v1, values: { uin: '1000000004' } } }
    expected: { lookup: match, derivationRuns: true, signed: true, facts: { uin: '1000000004', identity_status: active, alive: true }, value: true }
  - id: negative-inactive
    selectors: { subject: { profile: solmara-uin-v1, values: { uin: '1000000014' } } }
    expected: { lookup: match, derivationRuns: true, signed: true, value: false }
  - id: boundary-active
    selectors: { subject: { profile: solmara-uin-v1, values: { uin: '1000000004' } } }
    expected: { lookup: match, derivationRuns: true, signed: true, value: true }
  - id: missing-record
    selectors: { subject: { profile: solmara-uin-v1, values: { uin: '1000000098' } } }
    expected: { lookup: no_match, publicProblem: evidence.unavailable, derivationRuns: false, signed: false }
  - id: no-match
    selectors: { subject: { profile: solmara-uin-v1, values: { uin: '1000000099' } } }
    expected: { lookup: no_match, publicProblem: evidence.unavailable, derivationRuns: false, signed: false }
  - id: ambiguous-duplicate
    selectors: { subject: { profile: solmara-uin-v1, values: { uin: '1000000016' } } }
    expected: { lookup: ambiguous, publicProblem: evidence.unavailable, derivationRuns: false, signed: false }
  - id: source-failure
    sourceFailure: timeout
    expected: { publicProblem: source.unavailable, signed: false }
  - id: anti-reconstruction
    bundleMutation: duplicate-disclosure-family
    expected: { bundle: rejected }
privacyExpectation: { evidenceContains: [https://id.registrystack.org/solmara/concept/population-record-active], evidenceExcludes: [uin, identity_status, alive], diagnosticsExclude: ['1000000004'] }
