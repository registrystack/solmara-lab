fixture: registry.evidence.reference.solmara.mosd-child-benefit/v1
synthetic_only: true
common:
  observed_at: "2026-07-14T00:00:00Z"
  selectors: { subject: { profile: solmara-uin-v1, values: { uin: "1000000007" } } }
  derivationSelectorInputs: { subject: { profile: solmara-uin-v1, values: { uin: "1000000007" } } }
  expectedRequestParts: { query: [{ name: uin, value: "1000000007" }, { name: fields, value: "uin,duplicate_flag" }, { name: limit, value: "2" }], body: null }
  expectedTransport:
    path: /v1/datasets/mosd_enroll/entities/mosd_enroll/records
    fixedHeaders: [{ name: Accept, value: application/json }, { name: Data-Purpose, value: https://id.registrystack.org/solmara/purpose/child-benefit-review }]
cases:
  - id: positive
    response: { data: [{ uin: "1000000007", duplicate_flag: false }], pagination: { has_more: false } }
    expected: { lookup: match, derivationRuns: true, signed: true, value: true }
  - id: negative-subject-mismatch
    response: { data: [{ uin: "9999999999", duplicate_flag: false }], pagination: { has_more: false } }
    expected: { lookup: match, error: derivation_input_error, publicProblem: evidence_not_available, derivationRuns: true, signed: false }
  - id: boundary-already-enrolled
    response: { data: [{ uin: "1000000007", duplicate_flag: true }], pagination: { has_more: false } }
    expected: { lookup: match, derivationRuns: true, signed: true, value: false }
  - id: missing-fact
    response: { data: [{ uin: "1000000007" }], pagination: { has_more: false } }
    expected: { publicProblem: evidence_not_available, derivationRuns: false, signed: false }
  - id: no-match
    response: { data: [], pagination: { has_more: false } }
    expected: { lookup: no_match, publicProblem: evidence_not_available, derivationRuns: false, signed: false }
  - id: ambiguous
    response: { data: [{ uin: "1000000007", duplicate_flag: false }, { uin: "1000000007", duplicate_flag: true }], pagination: { has_more: true } }
    expected: { lookup: ambiguous, publicProblem: evidence_not_available, derivationRuns: false, signed: false }
  - id: source-failure
    sourceFailure: timeout
    expected: { publicProblem: dependency_unavailable, signed: false }
  - id: anti-reconstruction
    bundleMutation: duplicate-disclosure-family
    expected: { bundle: rejected }
privacyExpectation:
  evidenceContains: [https://id.registrystack.org/solmara/concept/not-already-enrolled]
  evidenceExcludes: [uin, duplicate_flag, "1000000007"]
  diagnosticsExclude: ["1000000007"]
