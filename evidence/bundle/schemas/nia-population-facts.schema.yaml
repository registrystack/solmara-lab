type: object
additionalProperties: false
required: [uin, identity_status, alive]
properties:
  uin: { type: string, minLength: 10, maxLength: 10 }
  identity_status: { type: string, minLength: 1, maxLength: 32 }
  alive: { type: boolean }
