audit:
  hash_secret_env: REGISTRY_NOTARY_AUDIT_HASH_SECRET
  sink: stdout
auth:
  api_keys:
  - fingerprint:
      name: PROGRAMME_CHILD_BENEFIT_CLIENT_TOKEN_HASH
      provider: env
    id: child-benefit-federator
    scopes:
    - mosd_programme:evidence_verification
deployment:
  profile: hosted_lab
evidence:
  allowed_purposes:
  - https://id.registrystack.org/solmara/purpose/child-benefit-review
  claims:
  - credential_profiles: []
    disclosure:
      allowed:
      - predicate
      - redacted
      default: predicate
      downgrade: deny
    evidence_mode:
      consultations:
        enrollment:
          inputs:
            uin: request.target.identifiers.solmara_uin
          outputs:
            duplicate_flag:
              nullable: false
              type: boolean
          profile:
            contract_hash: sha256:f390b9f014dedfb49f1743634033120bcf07f45c26b7c190a0303aaa4f24c5cf
            id: solmara-mosd-programme.child-benefit.enrollment
      type: registry_backed
    formats:
    - application/vnd.registry-notary.claim-result+json
    id: not-already-enrolled
    purpose: https://id.registrystack.org/solmara/purpose/child-benefit-review
    required_scopes:
    - mosd_programme:evidence_verification
    rule:
      bindings: {}
      expression: enrollment.matched && enrollment.duplicate_flag == false
      type: cel
    subject_type: person
    title: not already enrolled
    value:
      nullable: false
      type: boolean
    version: '1'
  credential_profiles: {}
  enabled: true
  max_credential_validity_seconds: 600
  relay:
    allow_insecure_localhost: false
    allowed_private_cidrs: []
    base_url: https://mosd-programme-relay.solmara.registrystack.org
    max_in_flight: 8
    token_file: /run/secrets/programme-notary-relay-token
    workload_client_id: programme-notary
  service_id: programme-notary
  variables: {}
instance:
  environment: hosted
  id: programme-notary
server:
  bind: 127.0.0.1:8081
  request_timeout: 30s
state:
  postgresql:
    connect_timeout_ms: 5000
    max_connections: 16
    operation_timeout_ms: 2000
    root_certificate_path: /etc/solmara/postgres/root.crt
    url_env: REGISTRY_NOTARY_POSTGRES_URL
  storage: postgresql
